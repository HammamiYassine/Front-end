version: "3.9"
volumes:
  my-volume:
    external: true
networks:
  mt-network:
    driver: bridge    
services:  
  frontend:
    build : .
    container_name: front-end 
    ports:
      - 8084:80
    networks:
      - mt-network
